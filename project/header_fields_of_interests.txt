Accept >> What content are accepted
Connection >> close on close
Content encoding >> refuse  chunked
Content-length >> Needed if body
Content-type >> refuse other than image and text
Cookie >> For bonus
Expect >> Refuse anything else than 100-contnue
Range >> store in and check for possibility
Transfer encoding >> Refuse chunked
Upgrade >> Error 505

Response Header
Server >> Me
Date >> Upgrade date
Accept Range >> if error of range type returned with bytes
Allow >> if 405 , send allowed method for the route
Connection >> If fatal error, close. otherwise keep-alive
Content-length >> If body 
Content type >> Parse extension , text/plain if not known
Set-Cookie >> for bunus
Transfer-Encoding >> none
Keep-Alive >> timeout=5, max=1
